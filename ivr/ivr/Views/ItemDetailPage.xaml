<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ivr.ViewModels"
             xmlns:model="clr-namespace:ivr.Models"
             x:Class="ivr.Views.ItemDetailPage"
             x:DataType="local:ItemDetailViewModel">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Delete" Command="{Binding DeleteCommand}" />
        <ToolbarItem Text="Finish" Command="{Binding FinishCommand}" />
    </ContentPage.ToolbarItems>
    <StackLayout Margin="30">
        <Entry Text="{Binding Title}" />
        <Entry Text="{Binding Data}" />
        <DatePicker Date="{Binding Date}"/>
        <TimePicker Time="{Binding Time}"/>
        <Frame>
            <ListView ItemsSource="{Binding Subitems}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:Item">
                        <ViewCell>
                            <Label Text="{Binding Title}"/> 
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
        <StackLayout Orientation="Horizontal">
            <Button Text="Cancel" Command="{Binding CancelCommand}" />
            <Button Text="Save" Command="{Binding SaveCommand}" />
        </StackLayout>
    </StackLayout>
</ContentPage>